linters:
  enable:
    - gofmt
    - govet
    - staticcheck
    - unused
    - errcheck
    - gosimple
    - ineffassign

linters-settings:
  govet:
    check-shadowing: true

issues:
  exclude-use-default: false
